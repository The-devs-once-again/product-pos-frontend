import ViewContainer from "components/containers/viewContainer";
import Typography from "components/typography";

const OrderList = () => {
  return (
    <ViewContainer>
      <Typography.headlineMedium>Orders</Typography.headlineMedium>
    </ViewContainer>
  );
};

export default OrderList;
