import ViewContainer from "components/containers/viewContainer";
import Typography from "components/typography";

const Menu = () => {
  return (
    <ViewContainer>
      <Typography.headlineMedium>Menu</Typography.headlineMedium>
    </ViewContainer>
  );
};

export default Menu;
